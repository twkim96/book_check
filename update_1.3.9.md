# file_check 1.3.9 — 안전한 단일 변경 대기열

- 상태: 구현 및 자동 회귀 완료, 운영 UI 확인 대기
- 기준 커밋: `b7fb5c8` (`fix: make v1.3.8 review pairs actionable`)
- 적용일: 2026-07-27
- 범위: 도서 관리 UI의 확인형 변경 작업 수락·실행·재확인 UX

## 1. 배경

1.3.8까지 실제 파일·DB를 변경하는 작업은 공통 mutation lock으로 한 번에 하나만 실행했다.
이 안전 경계는 필요하지만, 이미 격리 후 index 동기화가 진행 중일 때 관계 판정을 저장하면 새 요청을
보관하지 않고 `mutation lock is busy`로 즉시 거절했다. 사용자는 앞 작업이 끝난 시점을 추측해 같은
계획을 다시 만들어야 했다.

1.3.9는 병렬 변경을 허용하지 않는다. 대신 도서 관리 UI의 변경 의도를 지속 작업 레코드로 먼저
수락하고, 기존 단일 worker에서 순서대로 실행한다.

## 2. 실행 계약

```text
계획 확인
→ 변경 요청을 persistent job으로 등록
→ 단일 worker 대기열
→ 실행 차례에 계획·fingerprint·Doctor 재검증
→ 일치: 기존 journal mutation 실행
→ 불일치: 파일을 바꾸지 않고 needs_review
```

- 실제 mutation은 계속 `max_workers=1`과 기존 전역 mutation lock을 함께 사용한다.
- 폴더링·플랫폼 수집·Sheet 동기화 같은 서비스 원버튼은 자동 대기열에 쌓지 않는다. 실행 또는 대기
  중인 변경이 있으면 기존처럼 새 서비스 시작을 차단한다.
- 동일한 job 종류와 payload가 이미 활성 상태면 중복 job을 만들지 않고 기존 job을 돌려준다.
- 서버가 재시작되면 실행·대기 중이던 job을 자동 재실행하지 않고 `interrupted`로 남긴다.

## 3. 대기 중 상태 변경 처리

대기열 등록은 과거 계획에 실행 권한을 영구 부여하지 않는다. 각 handler는 자신의 기존 preview/plan을
다시 만들고 승인 당시의 `confirm_count`와 `plan_sha256`을 비교한다.

- 대상 파일이 격리·이동되어 활성 파일이 아니게 된 경우
- fingerprint 또는 source revision이 달라진 경우
- 계획 SHA, 대상 수, blocker가 달라진 경우

위 상황은 `failed`나 자동 강행이 아니라 다음 상태로 끝난다.

```text
state: needs_review
error.code: reconfirmation_required
파일·DB mutation: 없음
```

사용자는 원래 화면을 새로고침하고 현재 상태로 계획을 다시 확인하면 된다.

## 4. 대기열 적용 범위

- 제목 교정 재입고
- 분권 묶기
- 두 파일 관계 저장·정정·취소
- 사용자 승인 파일 격리
- 격리 복원·영구 삭제
- 파일 이름·위치 정리
- 관리 폴더 생성·등록·이동·전체 격리
- 작품 병합·분리·별칭 등록·별칭 해제
- 대표 파일 변경

## 5. UI

- 요청 수락 직후 `대기열 N번째`, 앞 작업 수를 표시한다.
- 작업 이력과 상세 화면에서 동적으로 바뀌는 대기 순서를 확인할 수 있다.
- 아직 `queued`인 작업만 상세 화면에서 취소할 수 있다.
- 실행을 시작한 작업은 중간 취소하지 않는다.
- `needs_review`는 원래 입력을 성공 처리하거나 지우지 않고 재확인 메시지를 표시한다.
- 관계 판정도 즉시 응답 API가 아니라 다른 변경과 같은 job 이력으로 추적한다.
- `distinct_work`는 제목이 완전히 같은 경우만이 아니라 비슷한 제목으로 잘못 엮인 서로 다른 작품도
  포함하므로 UI 문구를 `서로 다른 작품 (제목 동일·유사 포함)`으로 바로잡았다.
- 일반 관계 모달은 기본 판정을 미리 선택하지 않으며 사용자가 두 관계 중 하나를 명시적으로 고른다.
- `same_content`는 본문 동일 review를 확정하고 비대표 파일을 격리하는 내부·전문 계약으로 유지하되,
  일반 관계 모달에서는 숨기고 비교 화면의 사용자 승인 격리를 안내한다.

## 6. 검증 항목

- [x] 첫 변경이 실행 중일 때 두 번째·세 번째 변경이 순서대로 대기
- [x] 같은 활성 intent의 중복 등록 방지
- [x] 대기 중인 job만 취소되고 handler는 실행되지 않음
- [x] 관계 판정이 실행 중 격리 뒤 대기열에 정상 등록됨
- [x] 대기 중 관계 endpoint가 비활성화되면 판정 0건을 유지하고 `needs_review`
- [x] 서비스 원버튼의 기존 exclusive 차단 유지
- [x] 관계 저장 API가 job ID와 HTTP 202를 반환
- [x] 프런트엔드 production build
- [x] 전체 공개 Python 회귀: `253 passed`
- [x] Python `compileall`과 TypeScript/Vite production build
- [ ] 운영 서버 리로드 후 실제 UI에서 대기 순서·취소·재확인 표시 확인

## 7. 완료 기준

1. 같은 북서버에서 두 개 이상의 UI 변경 요청을 연속으로 눌러도 `mutation lock is busy`를 사용자에게
   노출하지 않는다.
2. 파일·DB 변경은 동시에 하나만 실행된다.
3. 대기 중 상태가 바뀐 계획은 자동 실행되지 않는다.
4. queued 취소, needs_review, succeeded가 작업 이력에서 구분된다.
5. 전체 공개 테스트, 프런트 빌드, Python compileall, `git diff --check`가 통과한다.
